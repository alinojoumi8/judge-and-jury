# Environment / secrets
.env

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor / local tooling
.DS_Store
.idea/
.vscode/
.claude/

# Local trial transcripts (may contain real case content) — kept off the repo
outputs/

# Private case fixtures (real matter) — kept local, never published.
# The generic samples/example_case.json IS tracked so the runner has a demo default.
# Broadened from smart_prime_case* to catch every real-matter fixture (fresh,
# grounded, trial_12jury, …), which the narrower pattern let slip through.
samples/smart_prime_*
